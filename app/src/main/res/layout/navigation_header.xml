<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:gravity="bottom"
    android:orientation="vertical"
    android:paddingBottom="@dimen/paddingOrMar_L"
    android:paddingLeft="@dimen/paddingOrMar_L"
    android:paddingRight="@dimen/paddingOrMar_L"
    android:paddingTop="@dimen/paddingOrMar_L"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="@dimen/navigation_header_icon"
        android:layout_height="@dimen/navigation_header_icon"
        android:layout_gravity="center"
        android:padding="@dimen/paddingOrMar_L"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        app:srcCompat="@drawable/inventory_icon" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/paddingOrMar_S"
        android:gravity="center"
        android:textColor="@android:color/black"
        android:text="@string/app_name"
        android:textStyle="bold"/>

</LinearLayout>